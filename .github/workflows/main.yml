name: CI

on:
  push:
    branches: 
        - master 


jobs:
  Build-Job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Echo Text
        run: echo "Hello, World!"

